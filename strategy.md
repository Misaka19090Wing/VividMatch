实现一个基于opencv的图像识别算法，要求能够识别不同分辨率下的相同图片，能够识别不同分辨率下的相同视频 。

| 功能模块           | 选型                                      | 职责                                                                                                   |
| ------------------ | ----------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| **视频解码** | **FFmpeg (libavformat/libavcodec)** | 解封装视频流，同时提取**视频帧**和 **音频PCM原始数据** （16kHz单声道），无需落盘临时文件。 |
| **图像处理** | **OpenCV C++**                      | 对视频帧进行缩放、灰度化、分块DCT（离散余弦变换），生成视觉指纹。                                      |
| **音频FFT**  | **FFTW (双精度)**                   | 对音频PCM进行短时傅里叶变换（STFT），生成频谱图或提取频域特征（Mel谱）。                               |
| **特征比对** | **手写C++（STL/算法）**             | 汉明距离计算、滑动窗口匹配、时序连续性（单调性）校验。                                                 |

1. 抗马赛克（视觉层）：多网格局部哈希
   将每一帧缩放到 64x64，切分为 4x4=16 个小块，对每个小块做 DCT 并生成哈希值。两帧比对时，剔除距离最大的4个块（马赛克/黑边区域），只用剩余12块的平均相似度，确保画面局部遮挡不影响整体判定。
2. 防混剪（时序层）：时间轴单调性校验
   提取视频的时间戳指纹序列（每秒1帧）。比对时不仅看匹配率，更关键的是检查：匹配到的库视频时间戳，是否随待测视频时间戳严格单调递增。若出现跳跃或回退，直接判定为“混剪拼接”，杜绝误报。
3. 抗干扰（音频层）：FFTW频域指纹
   使用 FFTW 对 16kHz 单声道 PCM 做 STFT，生成能量包络（RMS）和频谱质心作为音频指纹。这能无视视频分辨率和画面马赛克，若画面匹配低但音频匹配高，系统会启动“音频一票否决权”，召回被重度遮挡的相同视频。

### 最终决策逻辑（视听融合矩阵）

* **视觉高 + 音频高 + 时序递增** →  **完全相同的视频** （仅分辨率/码率不同）
* **视觉低 + 音频高** →  **画面被重度遮挡/马赛克** ，仍判定为相同视频
* **视觉高 + 音频低** →  **画面相同但BGM被替换** ，判定为二次创作（不同视频）
* **时序跳跃（无论分数）** →  **直接判定为混剪拼接** ，拒识
